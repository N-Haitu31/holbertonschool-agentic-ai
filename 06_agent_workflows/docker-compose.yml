services:
  task-poller:
    build: .
    container_name: task-poller
    network_mode: none
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    volumes:
      - ./tasks.json:/app/tasks.json:ro
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev